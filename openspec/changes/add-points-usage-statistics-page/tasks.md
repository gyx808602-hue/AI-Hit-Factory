## 1. 前置阅读与范围确认

- [x] 1.1 读取 `openspec/config.yaml`、`openspec/project.md`、本 change 的 `proposal.md`、`design.md` 和 `specs/points-usage-statistics-page/spec.md`，确认积分统计页范围。
- [x] 1.2 检查现有路由、应用壳、页面风格、任务记录页和表格实现方式，确认颜色、字体、卡片、按钮、表格和状态反馈全部复用当前项目现有样式。
- [x] 1.3 确认当前没有真实积分接口字段，实施时不得在页面 JSX 中散落临时字段，必须先定义类型和适配层。

## 2. 类型、Mock 与 API 契约

- [x] 2.1 新增积分账户摘要、积分使用记录、积分记录详情、查询参数和分页结果的 TypeScript 类型。
- [x] 2.2 新增积分 Mock 数据，覆盖收入、支出、退款、冻结、失败、处理中和空列表等状态。
- [x] 2.3 新增积分 API Client，提供获取积分摘要、获取使用记录分页列表、获取记录详情的方法。
- [x] 2.4 新增 React Query hooks，封装积分摘要、积分使用记录列表和记录详情查询，不在页面直接调用 request/fetch/axios。
- [x] 2.5 为 API Client 或数据适配函数补充测试，验证 Mock 字段能转换为页面稳定使用的领域类型。

## 3. 页面与交互实现

- [x] 3.1 新增积分统计页面，只借鉴参考图的区域排布，颜色与组件风格采用现有项目，实现顶部积分概览卡片和统计卡片。
- [x] 3.2 新增积分使用记录表格，展示预留字段并支持加载态、错误态、空态和分页态。
- [x] 3.3 新增筛选区，至少预留关键词、业务类型、变动方向、状态和时间范围筛选，并确保筛选后页码回到第一页。
- [x] 3.4 在表格操作列新增“详情”按钮，点击后打开 Drawer 或 Modal 展示该条记录详情。
- [x] 3.5 确保页面在 1024px 到 1279px 窄桌面宽度下可用，表格关键字段和“详情”按钮不被不可恢复地遮挡。

## 4. 路由、菜单与权限预留

- [x] 4.1 在静态路由注册表中新增积分统计页 route key、路径、页面组件映射和路由 meta。
- [x] 4.2 在菜单配置或动态菜单映射中预留积分统计入口，权限码建议使用 `points:usage:view`。
- [x] 4.3 为“详情”操作预留按钮级权限码 `points:usage:detail`，当前无权限体系接入时保持前端展示逻辑可扩展。
- [x] 4.4 验证刷新积分统计页路径后仍可恢复页面，不出现 404 或白屏。

## 5. 测试与验证

- [x] 5.1 新增积分页面渲染测试，覆盖积分概览、记录列表、空态和错误态。
- [x] 5.2 新增详情交互测试，验证点击“详情”按钮后展示对应记录信息，并可关闭。
- [x] 5.3 新增筛选和分页测试，验证查询参数变化会触发列表刷新并重置页码。
- [x] 5.4 新增路由注册测试，验证积分 route key 能映射到正确页面组件。
- [x] 5.5 执行 `npm run typecheck` 和相关 Vitest 测试，确认新增实现不破坏现有页面。

## 6. 文档与收尾

- [x] 6.1 更新 `doc/progress.md`，记录积分统计页实现进展、当前判断、下一步和验证结果。
- [x] 6.2 如实现阶段发现真实接口字段或菜单路径已确定，补充到本 change 文档或新增专题进展文档。
- [x] 6.3 完成后运行 OpenSpec 校验，确认 change 文档与规格可归档。
