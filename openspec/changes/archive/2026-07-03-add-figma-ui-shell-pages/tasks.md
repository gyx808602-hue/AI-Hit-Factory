## 1. 前置阅读与工程初始化

- [x] 1.1 读取 `openspec/config.yaml`、`openspec/project.md`、`doc/phase-one-ui-task-breakdown.md`、本 change 的 `proposal.md`、`design.md`、`specs/figma-ui-shell-pages/spec.md`。
- [x] 1.2 检查仓库是否已有前端工程；若没有，则创建 Vite + React + TypeScript 工程。
- [x] 1.3 安装并配置基础依赖：Ant Design、TailwindCSS、React Router、lucide-react、TypeScript、lint/build 工具。
- [x] 1.4 建立推荐目录边界：`src/app`、`src/pages`、`src/features`、`src/components`、`src/api`、`src/store`、`src/utils`，避免创建无实际内容的空目录。
- [x] 1.5 配置基础脚本：开发启动、类型检查、构建、lint。

## 2. 主题、路由与应用壳

- [x] 2.1 使用 Ant Design `ConfigProvider` 配置暗色主题、品牌色、圆角、字体和组件 token。
- [x] 2.2 配置 TailwindCSS，用于页面布局、间距、响应式和局部视觉微调。
- [x] 2.3 建立静态 route registry，定义 `RouteKey`、route meta、页面组件映射和基础导航配置。
- [x] 2.4 实现应用壳：左侧侧边栏、顶部栏、内容区域、滚动容器和全局背景。
- [x] 2.5 实现侧边栏折叠、激活菜单状态、顶部通知入口和用户入口的静态展示。
- [x] 2.6 预留 403、404、登录过期和后续动态菜单过滤扩展点。
- [x] 2.7 为应用壳补充窗口兼容基础：内容区 `min-width: 0`、页面级滚动容器、侧边栏折叠、低高度窗口滚动边界。

## 3. Mock 数据与类型边界

- [x] 3.1 定义首批 UI 类型：任务、素材、数字人、工作台指标、导航菜单、生成状态、视频生成模式。
- [x] 3.2 建立集中 Mock 数据模块，覆盖工作台统计、最近任务、任务记录、素材库、数字人列表。
- [x] 3.3 建立轻量 Mock API 或 feature-local typed source，避免同一类 mock 数据在多个页面重复硬编码。
- [x] 3.4 明确全局状态边界：侧边栏折叠、主题等客户端 UI 状态可全局保存；任务、素材、数字人等服务端型数据先保持 Mock 数据源，后续接 React Query。

## 4. 公共页面实现

- [x] 4.1 实现工作台页面：统计卡片、快速开始、最近任务和导航跳转。
- [x] 4.2 实现任务记录页面：统计摘要、搜索、类型筛选、状态筛选、任务列表、状态标签和操作按钮。
- [x] 4.3 实现素材库页面：类型筛选、搜索、上传入口、素材卡片网格、预览/下载/删除操作入口。
- [x] 4.4 为任务记录和素材库预留服务端分页、筛选、排序、懒加载和 React Query 缓存接入点。
- [x] 4.5 为表格和素材网格补充窗口兼容：宽表支持横向滚动，卡片网格支持不同窗口宽度下自动换列。

## 5. Figma 演示能力页面实现

- [x] 5.1 实现商品视频生成页面：商品信息、AI 卖点、脚本、生成模式、生成中和结果预览。
- [x] 5.2 实现图文生成视频页面：输入方式切换、主题、文案、图片上传占位、输出方式、开关项、生成结果。
- [x] 5.3 实现爆款视频改编 / 追爆页面：原创改编声明、上传状态、结构分析、三种改编模式和生成结果。
- [x] 5.4 实现数字人管理页面：统计、搜索、状态筛选、数字人卡片、添加弹窗、启用/停用和删除本地状态。
- [x] 5.5 检查这些页面的组件拆分，只有复用明确的区块才抽到公共组件；单页面专用区块留在 feature 内部。
- [x] 5.6 检查演示页面在 `1280x720` 和 `1366x768` 下的主按钮、预览区、步骤条、弹窗不会被遮挡或挤压变形。

## 6. 视觉与交互验收

- [x] 6.1 对照 Figma Make 原型检查应用壳、导航、主题、页面密度和关键交互状态。
- [x] 6.2 检查所有页面在常见桌面宽度下无明显文字溢出、按钮挤压、元素重叠和滚动异常。
- [x] 6.3 检查生成中、生成成功、失败、空态、禁用态和筛选无结果状态。
- [x] 6.4 检查任务记录和素材库不会一次性渲染不受控的大量 DOM，并记录后续真实数据接入时的分页/懒加载策略。
- [x] 6.5 使用浏览器窗口矩阵验证：`1280x720`、`1366x768`、`1440x900`、`1536x864`、`1920x1080`。
- [x] 6.6 验证紧凑窗口：`1024px - 1279px` 宽度下导航、表格、卡片和主要按钮可用。
- [x] 6.7 验证窄屏兜底：小于 `1024px` 宽度下不白屏、不重叠、不出现不可关闭弹窗。
- [x] 6.8 验证弹窗/抽屉在 `720px` 高度下有最大高度和内部滚动，主操作按钮可访问。

## 7. 自动化验证与收尾

- [x] 7.1 运行 TypeScript 类型检查。
- [x] 7.2 运行 lint 或格式检查。
- [x] 7.3 运行生产构建。
- [x] 7.4 启动本地开发服务器并使用浏览器检查首批页面无白屏、无运行时错误、导航可用，并完成窗口矩阵截图或记录。
- [x] 7.5 更新 `doc/progress.md`，记录本阶段完成内容、当前判断、下一步和遗留问题。
- [x] 7.6 运行 `openspec status --change add-figma-ui-shell-pages`，确认 change 已满足 apply 前置要求或记录未完成项。
