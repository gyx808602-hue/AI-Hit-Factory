# 2026-06-27 OpenSpec + Codex 移动端工作流进展

## 已完成

- 已确认本轮目标不是单一文档，而是为移动端开发建立一套同时覆盖 `OpenSpec` 与 `Codex` 的任务工作流。
- 已确认目标技术方向：
  - 移动端框架：`uni-app`
  - 技术栈：`Vue 3 + TypeScript`
  - 交付目标：同时兼容 `小程序` 与 `App`
- 已扫描目标仓库 [F:\AAA_AI_aisperce\AI-Hit-Factory](F:\AAA_AI_aisperce\AI-Hit-Factory)，确认仓库内已经存在：
  - `openspec/`
  - `doc/`
  - 多个 `openspec/changes/*` 变更示例
- 已识别一个关键现状差异：
  - 当前仓库代码主体仍然是 `React + TypeScript + Vite`
  - 后续不能只“追加移动端模板”，还要明确“移动端规范如何与现有 React 工程并存或迁移”
- 已确认新增一级约束：
  - 需要同时考虑 `小程序` 与 `App` 的兼容性
  - 工作流里必须纳入跨端能力、平台差异与验收检查点

## 当前判断

- 本轮更适合做“面向 uni-app 的移动端专用 OpenSpec + Codex 工作流扩展”，而不是推翻现有 `openspec` 目录重建。
- 兼容性会成为这套工作流的一级约束，至少需要覆盖：
  - 页面与组件的跨端写法边界
  - API、登录态、上传、权限、路由/页面栈在小程序与 App 的差异
  - 平台能力调用与条件编译策略
  - 真机、模拟器、开发者工具三类验证方式

## 下一步

1. 输出 2-3 套移动端工作流方案，并给出推荐方案。
2. 先让用户确认目录与流程设计，再落到 `openspec` 与 `doc` 的实际文件模板。
3. 若设计确认，再继续写 spec / plan / task 示例，并衔接 Codex 执行入口。

## 阶段更新：推荐方案确认中

### 已完成

- 已完成 3 套移动端工作流方案对比。
- 已给出推荐方案：保留现有 `openspec/`，在其基础上扩展 `uni-app + Vue3 + TS` 的移动端专用规范与 Codex 执行模板。

### 当前判断

- 最适合当前仓库的路径不是重建全新规范，而是在现有 `openspec` 体系上叠加移动端约束。
- 由于目标同时覆盖 `小程序 + App`，后续设计必须把兼容性矩阵作为一级产物，而不是只放在实现说明里带过。

### 下一步

1. 展开推荐方案的目录结构、文件职责与任务流设计。
2. 等用户确认设计方向后，再实际创建模板文件与示例变更。
