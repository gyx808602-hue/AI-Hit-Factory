## 2026-07-02 音色管理菜单乱码修复

### 已完成
- 已定位用户可见乱码 `闊宠壊绠＄悊`，实际位于：
  - `src/app/router/routeRegistry.tsx`
- 已将该菜单标题修复为：
  - `音色管理`
- 已复核 `vite.config.ts` 中代理注释，目前已是正常 UTF-8 中文：
  - `Target 保持最纯粹的根域名/IP`
  - `在重写规则里，把 /api 替换成后端实际需要的前缀`
- 已执行生产打包命令：
  - `npm run build`

### 验证结果
- `npm run build` 已通过。
- 构建输出仍有 `antd-vendor` 超过 `900 kB` 的体积 warning，这是既有分包体积提示，不是本次乱码修复引入的问题。
- 二次扫描后，业务源码中未再发现 `闊宠壊绠＄悊`。

### 当前判断
- 本次用户可见问题已修复：菜单展示应从乱码恢复为 `音色管理`。
- 扫描仍命中 `openspec/changes/connect-dynamic-menu-routes/tasks.md` 中的历史乱码内容，但该文件属于历史 OpenSpec 任务记录，本轮暂不修改，避免误改历史变更材料。

### 下一步建议
1. 如果你希望彻底清理历史文档乱码，可以单独开一轮“文档编码修复”任务。
2. 如果只关注页面展示，本轮已经完成。
